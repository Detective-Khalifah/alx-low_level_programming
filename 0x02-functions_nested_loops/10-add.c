#include "main.h"

/**
 * add - adds two integers and returns the result.
 * @a: first int number
 * @b: second int number
 * Return: the result of binary operation a + b.
 */
int add(int a, int b)
{
	return (a + b);
}

