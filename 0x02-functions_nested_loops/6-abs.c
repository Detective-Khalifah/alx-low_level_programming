#include "main.h"

/**
 * _abs - computes the absolute value of an int.
 * @num: the int to be computed.
 * Return: the absolute value.
 */
int _abs(int num)
{
	if (num < 0)
		num * -1;
	return (num);
}

