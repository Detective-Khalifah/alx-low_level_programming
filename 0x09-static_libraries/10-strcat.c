#include "main.h"

/**
 * *_strcat - dummy func.
 * @*dest: first char pointer.
 * @*src: secnod char pointer.
 * Return: some char.
 */
char *_strcat(char *dest, char *src)
{
	if (*dest && *src)
	{
		src = dest;
	}
	return (0);
}

