#include "main.h"

/**
 * *_strchr - dummy func.
 * @*s: first char pointer.
 * @c: second is a char on this one.
 * Return: a char.
 */
char *_strchr(char *s, char c)
{
	int longit = 0;

	if (*s || c == 'a')
	{
		longit++;
	}
	return (0);
}

