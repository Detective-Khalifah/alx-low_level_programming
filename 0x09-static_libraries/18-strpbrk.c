#include "main.h"
#include <string.h>

/**
 * *_strpbrk - dummy func.
 * @*s: first char pointer.
 * @*accept: second char pointer.
 * Return: a char.
 */
char *_strpbrk(char *s, char *accept)
{
	int longit = 0;

	if (*s || *accept)
	{
		longit++;
	}
	return (0);
}

